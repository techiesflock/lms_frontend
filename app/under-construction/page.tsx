import type { Metadata } from "next";
import { UnderConstructionBodyModern } from "@/components/dream-lms/pages/specialPagesModern";

export const metadata: Metadata = {
  title: "Under Construction | MetaWaves",
  description: "This section is being upgraded.",
};

export default function Page() {
  return <UnderConstructionBodyModern />;
}
